program Taller;

uses
  System.StartUpCopy,
  FMX.Forms,
  PanelMaestro in 'PanelMaestro.pas' {Form1};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1, Form1);
  Application.Run;
end.
